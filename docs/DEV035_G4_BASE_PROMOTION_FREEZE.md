# DEV035-G4 Base Promotion Freeze

Status: `BASE_PROMOTION_FROZEN_G4_RESEARCH_DESIGN_NEXT`

Date: 2026-09-02

## 1. Promotion authority

This promotion is authorized only because the permanently frozen
DEV034-G3B-R1 canonical result passed deep read-only verification.

DEV034-G3B-R1 canonical artifact:

`/home/emadh/Multi-Market/evidence/dev034_g3b_r1_common_support_screen_v1/DEV034_G3B_R1_COMMON_SUPPORT_SCREEN_RESULT.json`

Artifact SHA256:

`16200a1595d9472fe488740c0ab63e013b65824298ef1cb0b8856322416a8167`

Artifact bytes:

`873268`

Deep read-only verification:

- CHECKS_PASS = 49
- CHECKS_FAIL = 0
- G3C16_TRUE_SURVIVOR_IDENTITY = PASS
- G3C16_CAN_BECOME_NEXT_LAYERED_DEVELOPMENT_BASE = YES

Permanent rules:

`DEV034-G3A-R1 MUST NEVER BE RERUN`

`DEV034-G3B-R1 MUST NEVER BE RERUN`

## 2. New inherited layered-development base

The new historical-development base is:

`DEV030-P3 + DEV034-G3C16 FULL_FROZEN_R_CONTEXT`

Base feature composition:

- 23 exact DEV030-P3 PRICE32/S1 features
- 22 exact frozen R-context features from G3C16
- total feature count = 45

Support identity:

- common-support rows = 1341
- LONG = 665
- SHORT = 676

Support SHA256:

`caa61e84281061d00e4244e4f9b30ed2096e5acb95df9906aa7de0f28750ab75`

Label SHA256:

`fcb1b8f6c5f7994ca8c611cb3381146f401be7623ef36ae316a9a2e477a83385`

Full frozen R-context SHA256:

`b98239fdf22de77a476c7d4b13d4a677c06de101faedd42cbf8e11da0b145763`

## 3. Scientific basis for promotion

Matched comparator:

`P3_COMMON_SUPPORT_REFIT`

Comparator pooled BA:

`0.5365784523410725`

Promoted survivor:

`G3C16 FULL_FROZEN_R_CONTEXT`

G3C16 pooled BA:

`0.5920001546112814`

Incremental pooled delta BA:

`+0.05542170227020893`

Joint 16-candidate max-stat q95:

`0.04403844667199219`

Max-stat FWER p:

`0.0075`

G3C16 passed every preregistered strong-survivor gate.

## 4. Non-promoted G3 candidates

The following remain frozen and may not be silently promoted:

- G3C04 = G3_LAYER_INCONCLUSIVE
- G3C07 = G3_LAYER_INCONCLUSIVE
- G3C15 = G3_LAYER_INCONCLUSIVE

All other G3 candidates = G3_LAYER_REJECTED.

No inconclusive or rejected G3 candidate may be appended to the new base unless
a separately preregistered future experiment explicitly retests a new
scientific question without rewriting the frozen G3B-R1 outcome.

## 5. What promotion means

Promotion means only:

- future historical layered-development groups must compare incremental
  information against the inherited 45-feature base;
- the selected target remains A / 120s / 16bp;
- the direction-given-touch task remains unchanged;
- the existing common support remains the current inherited support unless a
  future group requires a separately preregistered fair common-support recovery.

Promotion does not mean:

- forward-holdout validation;
- PnL validation;
- economic profitability;
- execution-policy readiness;
- production readiness.

## 6. Forward and economic guards

Still prohibited:

- Sep-01+ analytical access
- Aug-30 reuse for DEV035 development
- PnL
- threshold optimization
- calibration rescue
- leverage
- Railway/bucket analytical access
- post-hoc G3C16 feature pruning
- adding G3C04/G3C07/G3C15 because they were close

## 7. Next action

DEV035-G4 must be a new coherent information family added on top of the
45-feature inherited base.

Before freezing the G4 candidate universe:

1. review prior failed/successful strategy groups;
2. inspect existing repository features to avoid duplication;
3. review relevant public literature/code for genuinely distinct causal
   information families;
4. choose one finite candidate group;
5. preregister exact candidates, support rules, folds, model lineage,
   multiplicity correction, survivor gate, and stop rule before any fit.

No G4 predictive fit is authorized by this base-promotion freeze.

Current state:

`DEV035_G4_BASE_PROMOTED_RESEARCH_AND_DESIGN_NEXT_NO_FIT`
