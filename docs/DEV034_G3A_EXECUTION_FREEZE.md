# DEV034-G3A Execution Freeze

Status: `EXECUTION_FROZEN_LOCAL_REAL_DATA_PREFLIGHT_REQUIRED`

Scientific implementation commit:

`ca7fc20811375098ef6a5dd29ce7fd0803ba8e36`

Successful CI run:

`33655507208 = SUCCESS`

All 15 workflow jobs passed, including:

- dev034-g3a-materialization = SUCCESS
- dev033-g2b-screen = SUCCESS
- dev033-g2a-materialization = SUCCESS
- dev032-e2b-screen = SUCCESS
- dev032-e2a-materialization = SUCCESS
- dev032-e1b-screen = SUCCESS
- dev032-e1b-r1-harness = SUCCESS
- retained DEV031 regressions = SUCCESS
- Python 3.10 unit tests = SUCCESS
- Python 3.12 unit tests = SUCCESS

No real DEV034-G3A materialization has occurred.

## Frozen base

DEV030-P3 remains the direction-stage base:

`A / 120s / 16bp / 32s / PRICE / S1`

## Frozen context universe

Exactly 22 pre-existing causal `R_FEATURE_NAMES` inherited from the frozen
EXP004/EXP022/EXP024 lineage.

Exactly 16 preregistered G3 candidate subsets.

G3A stores the 22 fields once per exact P3 support row and retains daily and
campaign matrix hashes for all 16 candidate subsets.

## Frozen historical inputs

Only the already-consumed Jan-Jul 2026 BTCUSDT Phase0DL FEATURES250 files.

All seven SHA256 identities are frozen in code.

No:

- August access
- Sep-01+
- Railway
- archive bucket
- abundant-love
- acquisition/download
- direction fit
- direction metric
- temporal null
- PnL

## No-support-shrink rule

Every exact P3 T1 support timestamp must have a valid finite frozen R-context
vector under the inherited EXP004 helper semantics.

If any required context is unavailable:

`G3A FAILS CLOSED`

No P3 row may be dropped.

## Canonical output

Directory:

`/home/emadh/Multi-Market/evidence/dev034_g3a_opportunity_volatility_context_v1`

Artifact:

`DEV034_G3A_OPPORTUNITY_VOLATILITY_CONTEXT.json`

After canonical G3A materialization starts:

`DEV034-G3A MUST NEVER BE RERUN`

## Process safety

- no parent-shell strict flags
- no bare exit
- dedicated console log
- canonical output/log must be absent before start
- read-only diagnosis after any canonical attempt
- no model fitting in G3A

## Next permitted action

Local real-data preflight only.

Current state:

`DEV034_G3A_EXECUTION_FROZEN_LOCAL_REAL_DATA_PREFLIGHT_REQUIRED`
