# DEV039 — Untouched Forward Confirmation Design

Status: `DESIGN_FROZEN_BEFORE_FORWARD_DATA_OPEN`

Date: 2026-09-03

## 1. Objective

Confirm the frozen integrated predictive policy on one untouched forward day
before any economic/PnL analysis.

This is a personal investment/profitability project. The purpose of DEV039 is
not to optimize prediction further; it is to decide whether the frozen signal
is credible enough to justify economic/execution testing.

## 2. Frozen predictive policy

Exactly:

`A0 PRICE32 + BTC45 + S0 TOUCH_ONLY_SELECTIVE + W720 rolling q80`

Target:

- BTCUSDT
- horizon = 120 seconds
- barrier = 16 bps

Controller:

- score = p_touch
- window = 720 prior scores
- quantile = 0.80
- numpy quantile method = higher
- current score excluded from its own threshold
- act iff current p_touch >= threshold
- direction = frozen BTC45 sign

No predictive knob may change.

## 3. Untouched forward period

Exactly:

`2026-09-01 UTC`

Reason:

The historical development lineage used the first day of each month:

- 2026-01-01
- 2026-02-01
- 2026-03-01
- 2026-04-01
- 2026-05-01
- 2026-06-01
- 2026-07-01

Therefore 2026-09-01 is the directly comparable untouched monthly forward day.

August is not eligible as fresh confirmation.

No date after 2026-09-01 may be substituted after results are seen.

## 4. Development data status

Jan-Jul are fully consumed development data.

They may be used only to train the already frozen models and initialize the
already frozen controller according to the rules below.

They may not be rescored to select a new model, feature set, controller,
threshold, target, or policy.

## 5. Final opportunity-model fit

Opportunity representation remains A0 PRICE32.

Frozen estimator lineage:

- StandardScaler
- LogisticRegression
- C grid = the frozen historical grid
- same deterministic tie-breaking as the A0 lineage

Final forward C selection:

1. inner fit = Jan-Jun;
2. inner validation = Jul;
3. select C using the exact frozen A0 selection criterion;
4. refit scaler + logistic model on Jan-Jul using the selected C;
5. generate Sep-01 p_touch only after the model is frozen.

No Sep-01 label may enter C selection, scaling, fitting, calibration, or
threshold selection.

## 6. Final BTC45 direction-model fit

Direction representation remains frozen BTC45.

Final direction C selection:

1. inner fit = Jan-Jun TOUCH rows;
2. inner validation = Jul TOUCH rows;
3. select C with the exact frozen BTC45 historical selection rule;
4. refit scaler + logistic model on Jan-Jul TOUCH rows using that C;
5. generate Sep-01 p_long without using Sep-01 direction labels.

No direction threshold search is allowed.

Direction decision remains:

- LONG iff p_long >= 0.5
- SHORT otherwise

## 7. W720 warm start

The W720 controller must start Sep-01 with the last 720 causally valid frozen
historical S0 p_touch scores from the July forward-like/OOS lineage.

No in-sample Jan-Jul score generated by the final Jan-Jul refit may be used as
a substitute warm buffer.

The warm buffer identity must be serialized and hashed before Sep-01
correctness labels are opened.

After Sep-01 begins:

- each current score is tested against the prior 720-score buffer;
- current score is appended only after its own decision;
- labels never enter the controller.

## 8. Stage P0 — forward support/feature audit only

DEV039-P0 may open Sep-01 raw inputs only for:

- source identity/hash;
- timestamps;
- feature availability;
- causal support construction;
- finite-matrix checks;
- warm-buffer identity;
- final-model fit and prediction generation without correctness scoring.

P0 must not calculate:

- TOUCH/NONE correctness metrics;
- action precision;
- correct/false actions;
- action-on-NONE fraction;
- temporal null;
- PnL;
- fees;
- spread cost;
- slippage;
- executable returns.

P0 passes only if:

- Sep-01 source identity is frozen;
- causal A0/BTC45 feature construction succeeds;
- no future-looking feature is required;
- forward support >= 1000 rows;
- all matrices/probabilities are finite;
- p_touch and p_long are in [0,1];
- W720 warm buffer has exactly 720 prior scores;
- forward action stream is deterministic and non-empty;
- no forward correctness label has been inspected.

If P0 fails, DEV039 stops. No substitute day is allowed.

## 9. Stage P1 — single forward correctness confirmation

Only after P0 is frozen PASS may Sep-01 target labels be opened.

Exactly one forward policy is scored.

No comparator controller and no challenger family exist in DEV039.

Serialize:

- support
- TOUCH/NONE counts
- action count
- abstain count
- coverage
- correct action count
- false action count
- action precision
- correct-action rate
- false-action rate
- LONG/SHORT counts
- LONG/SHORT precision
- acted-TOUCH direction accuracy
- action-on-NONE fraction
- threshold summary
- rolling coverage diagnostics

## 10. Frozen forward confirmation gates

DEV039-P1 passes only if ALL are true:

### Operational

1. support >= 1000 rows;
2. action count >= 100;
3. pooled/day coverage in [0.10, 0.30];
4. LONG count > 0;
5. SHORT count > 0.

### Correctness preservation

6. action precision >= 0.10;
7. correct-action rate >= 0.019;
8. false-action rate <= 0.176;
9. action-on-NONE fraction <= 0.82;
10. acted-TOUCH direction accuracy > 0.50.

These floors are frozen conservative preservation gates based on the already
consumed historical incumbent performance, not targets to be optimized on
Sep-01.

### Temporal falsification

11. observed action precision > the single-policy circular-shift null q95;
12. one-sided empirical temporal-null p <= 0.05.

## 11. Forward temporal null

Exactly:

- 1999 replicates
- seed = 20260903
- fixed action stream
- circularly shift the complete chronological Sep-01 three-class outcome
  sequence
- legal shifts from 30 through n-30 rows
- recompute action precision after each shift
- q95 method = higher
- plus-one empirical p denominator = 2000

This is a single-policy null; no multiple-comparison correction is needed
because no alternatives are tested.

## 12. Terminal outcomes

If every frozen gate passes:

`DEV039_FORWARD_CONFIRMATION_PASS`

The frozen policy is eligible to proceed to DEV038-B economic/execution
falsification.

If any frozen gate fails:

`DEV039_FORWARD_CONFIRMATION_FAIL`

Then:

- the policy is NOT forward-confirmed;
- no predictive rescue is permitted;
- no substitute forward day is permitted;
- no model/controller/threshold tuning is permitted;
- deployment is blocked.

The failed strategy may be archived, but not rescued using Sep-01 information.

## 13. Predictive search remains closed

Regardless of DEV039 result, there is NO:

- W search
- q search
- feature search
- model-family search
- new meta-filter
- target tuning
- direction-threshold tuning
- Sep-01-based retraining/tuning

## 14. Economic guards

DEV039 must not calculate:

- executable PnL
- fees
- spread cost
- slippage
- latency PnL
- gross bp/trade
- net bp/trade
- profit factor
- drawdown
- net return/day
- cost break-even

Those belong to the separately frozen DEV038-B protocol only after a forward
PASS.

## 15. Permanent upstream identity

Canonical DEV038-A-P2 parent:

- artifact =
  `/home/emadh/Multi-Market/evidence/dev038a_p2_final_controller_correctness_v1/DEV038A_P2_FINAL_CONTROLLER_CORRECTNESS_RESULT.json`
- SHA256 =
  `df32874a362cd75f646cdca483dc46956797431ac9a5861435639dfbf7f4b311`
- bytes = 191547
- execution commit =
  `a1ac3ea806def0f38b8952295b68fab8eb18e3a1`
- advanced controller = C2/W720

Permanent rule:

`DEV038-A-P2 MUST NEVER BE RERUN`

## 16. Execution discipline

Order:

1. freeze this design;
2. implement Sep-01 loader/final-fit/warm-buffer path;
3. synthetic/unit CI only;
4. execution freeze;
5. P0 no-correctness forward support/prediction audit;
6. freeze P0;
7. one P1 forward correctness run;
8. deep read-only verification;
9. freeze DEV039 result;
10. only if PASS, design DEV038-B economics.

No forward data is authorized by design freeze alone.

Current state:

`DEV039_FORWARD_CONFIRMATION_DESIGN_FROZEN_IMPLEMENTATION_NEXT_NO_FORWARD_DATA_OPEN`
